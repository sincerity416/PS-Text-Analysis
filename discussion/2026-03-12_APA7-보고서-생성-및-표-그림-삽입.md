# 2026-03-12 | APA 7 보고서 생성 및 표/그림 삽입

## 개요

1차 세션에서 완료한 R Tidy Text 분석 결과를 바탕으로,
APA 7 스타일의 Word 보고서를 생성하고 본문에 표 3개·그림 5개를 삽입한 작업 세션.

---

## 1. 문제 정의

### 배경
- 1차 세션 산출물: R 분석 스크립트, CSV 결과 4개, PNG 그림 5개
- 목표: 학술 보고서 형식(APA 7)으로 결과물을 통합하여 Word 문서 생성
- 추가 요구: 본문 흐름에 맞게 표(Table)와 그림(Figure)을 인라인 삽입

---

## 2. 의사결정 과정

### 2-1. 문서 생성 도구 선택

| 방식 | 검토 결과 |
|------|-----------|
| pandoc | `command not found` — Homebrew 미설치로 사용 불가 |
| **python-docx (선택)** | `pip3 install python-docx` 성공, APA 7 세부 스타일 제어 가능 |

**선택 근거:** pandoc은 markdown → docx 변환이 간단하나 APA 7 세부 서식(표 테두리, 들여쓰기 등) 제어가 어려움. python-docx는 코드로 세밀하게 제어 가능.

### 2-2. APA 7 표 서식 구현

python-docx의 기본 테이블은 전체 격자(grid) 형식이므로, APA 7 요건에 맞게 수동 조정 필요.

**APA 7 표 테두리 규칙:**
- 세로선(vertical border): 없음
- 가로선: 표 상단(top), 표 하단(bottom), 헤더 하단(header bottom)만 존재

**구현 방법:** OxmlElement를 사용하여 `w:tcBorders` 내 `w:left`, `w:right`, `w:insideV`를 `w:val="none"`으로 설정.

```python
def remove_table_borders(table):
    for row in table.rows:
        for cell in row.cells:
            tc = cell._tc
            tcPr = tc.get_or_add_tcPr()
            tcBorders = OxmlElement('w:tcBorders')
            for side in ['left', 'right', 'insideV']:
                border = OxmlElement(f'w:{side}')
                border.set(qn('w:val'), 'none')
                tcBorders.append(border)
            tcPr.append(tcBorders)
```

### 2-3. 참고문헌 Hallucination Check

30개 참고문헌을 WebSearch로 병렬 검증:
- **28개**: DOI 또는 저널 페이지 확인 완료
- **2개 수동 검증 권고:**
  - Chiumento et al. (2024) — 최신 논문, 검색 결과 제한적
  - Zehr & Pearson (2017) — 동명 논문 다수, 정확한 DOI 미확인
- 문서 내 두 항목에 `[수동 검증 필요]` 주석 포함

### 2-4. 표·그림 삽입 위치 결정

맥락적 적합성 기준으로 본문 내 삽입 위치 결정:

| 삽입 항목 | 위치 | 근거 |
|-----------|------|------|
| Table 1 | 서론(방법) 직후 | 분석 대상 논문 20편 개요 — 독자가 맥락 파악 후 바로 확인 |
| Table 2 | Bigram 빈도 분석 결과 직후 | 상위 빈도 bigram 수치를 본문 설명과 함께 제시 |
| Figure 1 | Table 2 직후 | 막대그래프로 빈도 분포 시각화 |
| Table 3 | TF-IDF 분석 결과 직후 | 논문별 특징 bigram 수치 제시 |
| Figure 2 | Table 3 직후 | TF-IDF 논문별 그래프 |
| Figure 3 | Figure 2 직후 | 논문별 bigram 비교 그래프 |
| Figure 4 | 네트워크 분석 결과 직후 | 단어 네트워크 시각화 |
| Figure 5 | 연도별 트렌드 결과 직후 | 시계열 트렌드 그래프 |

---

## 3. 해결한 이슈

### TF-IDF 표 노이즈 문제
**현상:** Table 3 원본 CSV에 인용문 노이즈 bigram 포함
- `asq june`, `lance frazier`, `copyright john` 등 참고문헌 파싱 부산물

**대응:** Python에서 알려진 노이즈 패턴을 필터링하여 표 데이터 구성
```python
noise_patterns = ['asq', 'june', 'lance', 'frazier', 'copyright', 'john wiley']
tfidf_clean = [row for row in tfidf_data if not any(p in row['bigram'] for p in noise_patterns)]
```

### python-docx 이미지 삽입 크기 조정
**현상:** 원본 PNG(14×10 인치)를 그대로 삽입 시 페이지 초과

**대응:** `add_picture()` 호출 시 `width=Inches(6.0)` 고정값 사용 (Letter 용지 마진 고려)

---

## 4. 생성된 산출물

| 구분 | 파일 | 설명 |
|------|------|------|
| 스크립트 | `scripts/generate_report.py` | python-docx APA 7 문서 생성기 |
| 보고서 | `2026-03-12_PS-TidyText-Analysis-Report.docx` | 표 3개·그림 5개 포함 최종 보고서 |

**보고서 구성:**
- 제목, 초록, 키워드
- 서론 (심리적 안전감 이론적 배경)
- 방법 (분석 대상 + Table 1)
- 결과 (Bigram·TF-IDF·네트워크·연도별 트렌드 + Table 2,3 + Figure 1–5)
- 논의
- 결론
- 참고문헌 (30개, APA 7 hanging indent)

---

## 5. GitHub 커밋 이력

| 커밋 | 내용 |
|------|------|
| `07c6713` | Add inline tables and figures to APA 7 Word report |
| `b66fa52` | Update CLAUDE.md with Word report session log |
